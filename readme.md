# Check for vulnerabilites in steam
### keywords 
* "Kubernetes"
* "Jenkins"

if vulnerabilites found
* creates a log
* a new branch
* a PR 

### pipeline
* if PR exist - exit gracefully
* if no PR, run script
* exit Gracefully 